"use client";

import { useState } from "react";
import { FlaskConical, Plus, ShieldCheck } from "lucide-react";
import type { RulePatch } from "@/lib/domain";
import { validateSchedule } from "@/lib/validator";
import { useWorkspace } from "@/components/workspace-provider";

export function ScenariosView(){
  const {state,currentAssignments,currentRulebookVersion,currentScheduleVersion,createScenario}=useWorkspace();
  const [name,setName]=useState("What-if scenario"); const [ruleId,setRuleId]=useState(""); const [newValue,setNewValue]=useState(""); const [notice,setNotice]=useState("");
  if(!state)return null;
  const selected=state.rules.find(r=>r.id===ruleId);
  const scalar=selected?Object.entries(selected.parameters).find(([,v])=>typeof v==="number"||typeof v==="string"):undefined;
  let patch:RulePatch|null=null;
  if(selected&&scalar&&newValue){const [key,old]=scalar;const value=typeof old==="number"?Number(newValue):newValue;patch={id:"scenario-draft",ruleId:selected.id,operation:"UPDATE",changes:{parameters:{...selected.parameters,[key]:value}},reason:`Scenario: ${key} ${String(old)} → ${String(value)}`,proposedBy:"USER"};}
  const preview=patch&&selected?validateSchedule({...state,rules:state.rules.map(r=>r.id===selected.id?{...r,...patch.changes}:r)},currentAssignments):null;
  async function create(){const result=await createScenario(name,patch?[patch]:[],[]);setNotice(result.ok?"Scenario saved without changing canonical truth.":result.error||"Could not create scenario.");if(result.ok){setName("What-if scenario");setRuleId("");setNewValue("");}}
  return <div className="space-y-6"><div><p className="text-sm leading-6 text-slate-600">Scenarios branch from specific Rulebook and Schedule versions. Temporary patches stay isolated until a later explicit Adopt workflow applies them.</p><div className="mt-2 flex gap-2 text-xs"><span className="rounded-full bg-slate-950 px-3 py-1.5 font-semibold text-white">Rulebook v{currentRulebookVersion}</span><span className="rounded-full border border-slate-200 bg-white px-3 py-1.5">Schedule v{currentScheduleVersion}</span></div></div>{notice?<div className="rounded-xl border border-blue-200 bg-blue-50 p-3 text-sm text-blue-900">{notice}</div>:null}<section className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2"><Plus className="size-5 text-slate-400"/><h2 className="font-semibold">Create a rule what-if</h2></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-600">Scenario name<input value={name} onChange={e=>setName(e.target.value)} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label><label className="text-xs font-semibold text-slate-600">Rule<select value={ruleId} onChange={e=>{setRuleId(e.target.value);setNewValue("")}} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm font-normal"><option value="">No temporary rule change</option>{state.rules.filter(r=>Object.values(r.parameters).some(v=>typeof v==="number"||typeof v==="string")).map(r=><option key={r.id} value={r.id}>{r.title}</option>)}</select></label></div>{selected&&scalar?<div className="mt-3 grid gap-3 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-3 text-sm"><span className="text-xs font-semibold text-slate-500">Current parameter</span><p className="mt-1"><strong>{scalar[0]}</strong> = {String(scalar[1])}</p></div><label className="text-xs font-semibold text-slate-600">Temporary value<input value={newValue} onChange={e=>setNewValue(e.target.value)} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label></div>:null}{preview?<div className={`mt-3 rounded-xl border p-3 text-sm ${preview.valid?"border-emerald-200 bg-emerald-50 text-emerald-900":"border-red-200 bg-red-50 text-red-900"}`}><ShieldCheck className="mr-2 inline size-4"/>Scenario result: {preview.hardViolations} hard violation(s), {preview.warnings} warnings. Canonical Rulebook remains unchanged.</div>:null}<button onClick={()=>void create()} className="mt-4 inline-flex min-h-11 items-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white"><FlaskConical className="size-4"/>Save scenario</button></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{state.scenarios.map(s=><article key={s.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2"><FlaskConical className="size-4 text-slate-400"/><h2 className="font-semibold">{s.name}</h2></div><p className="mt-3 text-xs text-slate-500">Based on Rulebook v{s.baseRulebookVersion} + Schedule v{s.baseScheduleVersion}</p><p className="mt-3 text-sm text-slate-600">{s.rulePatches.length} rule patch{ s.rulePatches.length===1?"":"es"} · {s.schedulePatches.length} schedule patch{ s.schedulePatches.length===1?"":"es"}</p><p className="mt-2 text-xs text-slate-400">Created {new Date(s.createdAt).toLocaleString()}</p></article>)}</section>{state.scenarios.length===0?<div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center text-sm text-slate-500">No scenarios yet. Create one above to test a “what if” without changing reality.</div>:null}</div>;
}
