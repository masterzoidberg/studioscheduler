"use client";

import { useState } from "react";
import { Archive, Bot, Download, History, Plus, Search, ShieldCheck, SlidersHorizontal, X } from "lucide-react";
import type { RulePatch, RuleType, StudioRule } from "@/lib/domain";
import { ruleClassification, validateSchedule } from "@/lib/validator";
import { useWorkspace } from "@/components/workspace-provider";

const statuses:StudioRule["status"][]=["ACTIVE","NEEDS_REVIEW","DISABLED","RETIRED"];
const reviewStatuses:NonNullable<StudioRule["reviewStatus"]>[]=["VERIFIED","NEEDS_REVIEW","UNVERIFIED"];
const ruleTypes:RuleType[]=["REQUIRED_ROOM","PREFERRED_ROOM","TEACHER_QUALIFICATION","TEACHER_UNAVAILABLE","TEACHER_AVAILABLE_WINDOW","REQUIRED_TEACHER","PREFERRED_TEACHER","MAX_TEACHER_GAP","MAX_TEACHER_WORKDAYS","MAX_STUDENT_GAP","MAX_STUDENT_ATTENDANCE_DAYS","MIN_STUDENT_ATTENDANCE_DAYS","LATEST_FINISH","EARLIEST_START","DIRECTLY_AFTER","NO_OVERLAP","FIXED_ASSIGNMENT","ROOM_CAPACITY","ROOM_CAPACITY_EXCEPTION","REQUIRED_LOWER_LEVEL","NO_DAY","PREFERRED_DAY","AVOID_DAY","RELATIONSHIP_ARRIVAL_WINDOW"];
const classifications=["HARD","VERY STRONG","STRONG","MODERATE","LIGHT","PREFERENCE","BASELINE","INFO","FLEXIBLE","NEEDS REVIEW","EXCEPTION","HARD ASSUMPTION","PRIORITY 1","PRIORITY 2","PRIORITY 3","PRIORITY 4","PRIORITY 5","PRIORITY 6","PRIORITY 7","PRIORITY 8","PRIORITY 9"];
type Filter="ALL"|"HARD"|"PREFERENCES"|"NEEDS_REVIEW"|"TEACHER"|"DANCER"|"ROOM"|"CLASS_STRUCTURE"|"SEQUENCING"|"OPTIMIZATION"|"RECENT";
const filters:Array<[Filter,string]>=[["ALL","All"],["HARD","Hard"],["PREFERENCES","Preferences"],["NEEDS_REVIEW","Needs Review"],["TEACHER","Teacher"],["DANCER","Dancer"],["ROOM","Room"],["CLASS_STRUCTURE","Class Structure"],["SEQUENCING","Sequencing"],["OPTIMIZATION","Optimization"],["RECENT","Recently Changed"]];

function badgeClass(value:string){const c=value.toUpperCase();return c==="HARD"?"border-red-200 bg-red-50 text-red-700":c.includes("STRONG")?"border-amber-200 bg-amber-50 text-amber-800":c.startsWith("PRIORITY")?"border-violet-200 bg-violet-50 text-violet-800":c==="INFO"||c==="FLEXIBLE"?"border-blue-200 bg-blue-50 text-blue-800":"border-slate-200 bg-slate-50 text-slate-700";}
function matches(rule:StudioRule,filter:Filter,recent:Set<string>){const c=ruleClassification(rule).toUpperCase();const cat=rule.category.toLowerCase();if(filter==="HARD")return c==="HARD";if(filter==="PREFERENCES")return ["VERY STRONG","STRONG","MODERATE","LIGHT","PREFERENCE","BASELINE"].includes(c)||c.startsWith("PRIORITY");if(filter==="NEEDS_REVIEW")return (rule.reviewStatus??rule.verificationStatus)!=="VERIFIED";if(filter==="TEACHER")return cat.startsWith("teachers");if(filter==="DANCER")return cat.includes("dancer");if(filter==="ROOM")return cat.includes("room");if(filter==="CLASS_STRUCTURE")return cat.includes("class structure")||cat.includes("curriculum");if(filter==="SEQUENCING")return cat.includes("sequencing");if(filter==="OPTIMIZATION")return cat.includes("optimization");if(filter==="RECENT")return recent.has(rule.id);return true;}

function mutableRuleChanges(rule:StudioRule):Partial<StudioRule>{return {id:rule.id,category:rule.category,type:rule.type,title:rule.title,description:rule.description,classificationRaw:rule.classificationRaw,strength:rule.strength,status:rule.status,verificationStatus:rule.verificationStatus,reviewStatus:rule.reviewStatus,review:rule.review,affectedEntityIds:rule.affectedEntityIds,parameters:rule.parameters,exceptions:rule.exceptions,enforcementStatus:rule.enforcementStatus,sourceRaw:rule.sourceRaw};}

function parameterFields(rule:StudioRule,setRule:(next:StudioRule)=>void){
  if(!rule.type)return <div className="rounded-xl border border-dashed border-slate-300 bg-slate-50 p-3 text-xs leading-5 text-slate-600"><strong>Not machine-typed yet.</strong> Reviewed wording remains authoritative. Structured enforcement can be added later without rewriting it.</div>;
  const fields:Array<[string,string,"text"|"number"|"time"]>=[];
  if(rule.type==="MAX_TEACHER_WORKDAYS")fields.push(["max_days","Maximum teaching days","number"]);
  if(rule.type==="MAX_TEACHER_GAP"||rule.type==="MAX_STUDENT_GAP"||rule.type==="RELATIONSHIP_ARRIVAL_WINDOW")fields.push(["minutes","Minutes","number"]);
  if(rule.type==="LATEST_FINISH"||rule.type==="EARLIEST_START")fields.push(["time","Time","time"]);
  if(["TEACHER_UNAVAILABLE","TEACHER_AVAILABLE_WINDOW","NO_DAY","PREFERRED_DAY","AVOID_DAY"].includes(rule.type))fields.push(["day","Day","text"]);
  if(rule.type==="TEACHER_AVAILABLE_WINDOW")fields.push(["start","Window start","time"],["end","Window end","time"]);
  if(rule.type==="REQUIRED_ROOM"||rule.type==="PREFERRED_ROOM")fields.push(["required_room_id","Room ID","text"]);
  if(["TEACHER_QUALIFICATION","TEACHER_UNAVAILABLE","TEACHER_AVAILABLE_WINDOW","MAX_TEACHER_WORKDAYS","REQUIRED_TEACHER","PREFERRED_TEACHER","RELATIONSHIP_ARRIVAL_WINDOW"].includes(rule.type))fields.push(["teacher_id","Teacher ID","text"]);
  if(rule.type==="ROOM_CAPACITY")fields.push(["room_id","Room ID","text"],["capacity","Capacity","number"]);
  if(rule.type==="DIRECTLY_AFTER")fields.push(["before_class_id","First class ID","text"],["after_class_id","Following class ID","text"]);
  if(rule.type==="RELATIONSHIP_ARRIVAL_WINDOW")fields.push(["student_id","Dancer ID","text"]);
  if(!fields.length)return <div className="rounded-xl bg-slate-50 p-3 text-xs text-slate-500">This machine rule type does not yet have a dedicated scalar editor.</div>;
  const set=(key:string,value:unknown)=>setRule({...rule,parameters:{...rule.parameters,[key]:value}});
  return <div className="grid gap-3 sm:grid-cols-2">{fields.map(([key,label,type])=><label key={key} className="text-xs font-semibold text-slate-600">{label}<input type={type} value={String(rule.parameters[key]??"")} onChange={e=>set(key,type==="number"?Number(e.target.value):e.target.value)} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label>)}</div>;
}

export function RulebookView(){
  const {state,currentAssignments,currentRulebookVersion,applyRulePatch,exportPackage,canEdit}=useWorkspace();
  const [query,setQuery]=useState("");const [filter,setFilter]=useState<Filter>("ALL");const [editing,setEditing]=useState<StudioRule|null>(null);const [original,setOriginal]=useState<StudioRule|null>(null);const [historyId,setHistoryId]=useState<string|null>(null);const [reason,setReason]=useState("");const [saving,setSaving]=useState(false);const [notice,setNotice]=useState("");
  if(!state)return null;
  const currentVersion=state.rulebookVersions.find(v=>v.status==="CURRENT");
  const verified=state.rules.filter(r=>(r.reviewStatus??r.verificationStatus)==="VERIFIED").length;
  const hard=state.rules.filter(r=>ruleClassification(r)==="HARD"&&r.status==="ACTIVE").length;
  const recentIds=new Set(state.ruleHistory.filter(h=>h.rulebookVersion===currentRulebookVersion).map(h=>h.ruleId));
  const filtered=state.rules.filter(rule=>`${rule.id} ${rule.title} ${rule.description} ${rule.category} ${ruleClassification(rule)}`.toLowerCase().includes(query.toLowerCase())&&matches(rule,filter,recentIds));
  const preview=editing&&original?validateSchedule({...state,rules:state.rules.map(r=>r.id===editing.id?editing:r)},currentAssignments):null;
  const historyRule=historyId?state.rules.find(r=>r.id===historyId):null;
  const history=historyId?state.ruleHistory.filter(h=>h.ruleId===historyId).sort((a,b)=>b.rulebookVersion-a.rulebookVersion):[];

  function beginEdit(rule:StudioRule){if(!canEdit)return;setEditing(structuredClone(rule));setOriginal(rule);setReason("");setNotice("");}
  function beginCreate(){if(!canEdit)return;const rule:StudioRule={id:`NEW-${String(Date.now()).slice(-3)}`,category:"General",type:null,title:"New scheduling rule",description:"Describe the scheduling policy in plain language.",strength:null,classificationRaw:"MODERATE",status:"NEEDS_REVIEW",verificationStatus:"UNVERIFIED",reviewStatus:"UNVERIFIED",review:{decision:"USER_EDIT",verified:false},affectedEntityIds:[],parameters:{},exceptions:[],source:{type:"USER_EDIT"},sourceRaw:{},enforcementStatus:"NOT_IMPLEMENTED",versionIntroduced:currentRulebookVersion+1,updatedAt:""};setEditing(rule);setOriginal(null);setReason("");setNotice("");}
  async function save(){if(!editing)return;setSaving(true);const patch:RulePatch={id:"rulebook-edit",ruleId:original?.id,operation:original?"UPDATE":"CREATE",changes:mutableRuleChanges(editing),reason:reason.trim()||(original?"Rule edited in Rulebook":"Rule created in Rulebook"),proposedBy:"USER"};const result=await applyRulePatch(patch);setSaving(false);if(!result.ok){setNotice(result.error||"Save failed.");return;}setNotice(`Saved as Rulebook v${result.version}. Schedule revalidation is required before the next schedule edit.`);setEditing(null);setOriginal(null);}
  async function changeStatus(rule:StudioRule,operation:RulePatch["operation"]){const result=await applyRulePatch({id:"rule-status",ruleId:rule.id,operation,changes:{},reason:`${operation.toLowerCase()} ${rule.title}`,proposedBy:"USER"});setNotice(result.ok?`Rulebook v${result.version} created.`:result.error||"Change failed.");}
  function download(){const pkg=exportPackage();if(!pkg)return;const blob=new Blob([JSON.stringify(pkg,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download=`DWDE-Rulebook-v${currentRulebookVersion}.json`;a.click();URL.revokeObjectURL(url);}
  function ask(rule:StudioRule){window.dispatchEvent(new CustomEvent("dwde:copilot",{detail:{message:`Explain current Rulebook rule ${rule.id} (${rule.title}), including its reviewed wording and machine-enforcement status.`}}));}

  return <div className="space-y-5">
    <section className="rounded-3xl border border-slate-200 bg-white p-5 sm:p-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-slate-500">DWDE 2026–2027 Master Rulebook</p><h2 className="mt-1 text-2xl font-semibold">Current Version: V{currentRulebookVersion}</h2><div className="mt-3 flex flex-wrap gap-2 text-xs"><span className="rounded-full bg-emerald-50 px-3 py-1.5 font-semibold text-emerald-700">Status: {currentVersion?.sourceHash?"Reviewed":"Current"}</span><span className="rounded-full border border-slate-200 px-3 py-1.5">{state.rules.length} Rules</span><span className="rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-emerald-700">{verified} Verified</span><span className="rounded-full border border-red-200 bg-red-50 px-3 py-1.5 text-red-700">{hard} active HARD</span></div>{currentVersion?.sourceHash?<p className="mt-3 max-w-3xl break-all text-[11px] leading-5 text-slate-400">Reviewed source fingerprint: {currentVersion.sourceHash}</p>:null}</div><div className="flex gap-2"><button onClick={download} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-slate-300 px-3 text-sm font-semibold"><Download className="size-4"/>Export</button>{canEdit?<button onClick={beginCreate} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-slate-950 px-4 text-sm font-semibold text-white"><Plus className="size-4"/>Add Rule</button>:null}</div></div><div className="mt-5 rounded-xl border border-blue-200 bg-blue-50 p-3 text-xs leading-5 text-blue-900"><ShieldCheck className="mr-1 inline size-4"/><strong>Human review and machine enforcement are separate.</strong> The reviewed Rulebook remains editable, but only explicit versioned mutations can change canonical truth.</div></section>
    {notice?<div className="rounded-xl border border-blue-200 bg-blue-50 p-3 text-sm text-blue-900">{notice}</div>:null}
    {!canEdit?<div className="rounded-xl border border-slate-200 bg-white p-3 text-sm text-slate-600">Viewer access is read-only. You can search, inspect history, export, and ask Copilot.</div>:null}
    <div className="rounded-2xl border border-slate-200 bg-white p-3"><div className="relative"><Search className="absolute left-3 top-3 size-4 text-slate-400"/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search Saturday closing, Karly, Tap 1, Aimee…" className="min-h-10 w-full rounded-xl border border-slate-200 pl-9 pr-3 text-sm"/></div><div className="mt-3 flex gap-2 overflow-x-auto pb-1">{filters.map(([value,label])=><button key={value} onClick={()=>setFilter(value)} className={`min-h-9 shrink-0 rounded-lg px-3 text-xs font-semibold ${filter===value?"bg-slate-950 text-white":"border border-slate-200 bg-white text-slate-600"}`}>{label}</button>)}</div></div>
    <div className="text-xs text-slate-500">Showing {filtered.length} of {state.rules.length} rules</div>
    <div className="grid gap-3">{filtered.map(rule=>{const classification=ruleClassification(rule);const reviewStatus=rule.reviewStatus??rule.verificationStatus;return <article key={rule.id} className="rounded-2xl border border-slate-200 bg-white p-4 sm:p-5"><div className="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full border px-2.5 py-1 text-[11px] font-bold ${badgeClass(classification)}`}>{classification}</span><span className="rounded-full border border-slate-200 px-2.5 py-1 text-[11px] font-semibold text-slate-600">{rule.status}</span><span className={`rounded-full px-2.5 py-1 text-[11px] font-semibold ${reviewStatus==="VERIFIED"?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-800"}`}>{reviewStatus}</span><span className="text-[11px] text-slate-400">{rule.enforcementStatus||"NOT_IMPLEMENTED"}</span></div><h3 className="mt-3 text-base font-semibold sm:text-lg">{rule.title}</h3><p className="mt-1 text-sm leading-6 text-slate-700">{rule.description}</p><div className="mt-3 flex flex-wrap gap-2 text-[11px] text-slate-500"><span className="rounded-lg bg-slate-100 px-2 py-1">{rule.id}</span><span>{rule.category}</span>{rule.type?<span className="rounded-lg bg-violet-50 px-2 py-1 text-violet-700">{rule.type}</span>:null}</div></div><div className="grid shrink-0 grid-cols-2 gap-2 sm:flex"><button onClick={()=>ask(rule)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-xl border border-slate-200 px-3 text-xs font-semibold"><Bot className="size-4"/>Ask Copilot</button><button onClick={()=>setHistoryId(rule.id)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-xl border border-slate-200 px-3 text-xs font-semibold"><History className="size-4"/>History</button>{canEdit?<button onClick={()=>beginEdit(rule)} className="inline-flex min-h-10 items-center justify-center gap-1.5 rounded-xl bg-slate-950 px-3 text-xs font-semibold text-white"><SlidersHorizontal className="size-4"/>Edit / Test</button>:null}</div></div>{canEdit&&rule.status!=="RETIRED"?<div className="mt-4 flex gap-4 border-t border-slate-100 pt-3">{rule.status==="DISABLED"?<button onClick={()=>void changeStatus(rule,"ENABLE")} className="text-xs font-semibold text-emerald-700">Enable rule</button>:<button onClick={()=>void changeStatus(rule,"DISABLE")} className="text-xs font-semibold text-slate-500">Disable temporarily</button>}<button onClick={()=>void changeStatus(rule,"RETIRE")} className="inline-flex items-center gap-1 text-xs font-semibold text-slate-500"><Archive className="size-3"/>Retire</button></div>:null}</article>})}</div>

    {editing?<div className="fixed inset-0 z-[70] flex items-end justify-center bg-slate-950/40 sm:items-center sm:p-6"><div className="max-h-[94vh] w-full max-w-2xl overflow-y-auto rounded-t-[28px] bg-white p-5 shadow-2xl sm:rounded-[28px] sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.14em] text-slate-500">{original?"Edit / test rule":"Create rule"}</p><h2 className="mt-1 text-xl font-semibold">{editing.title}</h2></div><button onClick={()=>setEditing(null)} className="rounded-xl p-2"><X className="size-5"/></button></div><div className="mt-5 grid gap-4"><label className="text-xs font-semibold text-slate-600">Stable ID<input disabled={Boolean(original)} value={editing.id} onChange={e=>setEditing({...editing,id:e.target.value.toUpperCase()})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal disabled:bg-slate-50"/></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-xs font-semibold text-slate-600">Title<input value={editing.title} onChange={e=>setEditing({...editing,title:e.target.value})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label><label className="text-xs font-semibold text-slate-600">Category<input value={editing.category} onChange={e=>setEditing({...editing,category:e.target.value})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label></div><label className="text-xs font-semibold text-slate-600">Current wording<textarea value={editing.description} onChange={e=>setEditing({...editing,description:e.target.value})} rows={4} className="mt-1 w-full rounded-xl border border-slate-300 p-3 text-sm font-normal"/></label><div className="grid gap-3 sm:grid-cols-3"><label className="text-xs font-semibold text-slate-600">Classification<input list="dwde-classifications" value={editing.classificationRaw||""} onChange={e=>setEditing({...editing,classificationRaw:e.target.value})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/><datalist id="dwde-classifications">{classifications.map(x=><option key={x} value={x}/>)}</datalist></label><label className="text-xs font-semibold text-slate-600">Status<select value={editing.status} onChange={e=>setEditing({...editing,status:e.target.value as StudioRule["status"]})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm font-normal">{statuses.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-xs font-semibold text-slate-600">Review status<select value={editing.reviewStatus??editing.verificationStatus} onChange={e=>setEditing({...editing,reviewStatus:e.target.value as StudioRule["reviewStatus"],verificationStatus:e.target.value as StudioRule["verificationStatus"]})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm font-normal">{reviewStatuses.map(x=><option key={x}>{x}</option>)}</select></label></div><div className="rounded-2xl border border-slate-200 p-4"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Machine enforcement</p><p className="mt-1 text-xs text-slate-500">Typed metadata augments reviewed wording; it never replaces it.</p></div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-semibold">{editing.enforcementStatus}</span></div><label className="mt-3 block text-xs font-semibold text-slate-600">Rule type<select value={editing.type||""} onChange={e=>setEditing({...editing,type:(e.target.value||null) as RuleType|null})} className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm font-normal"><option value="">Not machine-typed</option>{ruleTypes.map(x=><option key={x}>{x}</option>)}</select></label><div className="mt-3">{parameterFields(editing,setEditing)}</div></div>{editing.review?.original_text?<div className="rounded-xl border border-violet-200 bg-violet-50 p-3 text-xs leading-5 text-violet-950"><strong>Human review trail</strong><p className="mt-2"><strong>Draft:</strong> {editing.review.original_text}</p>{editing.review.correction_raw?<p className="mt-1"><strong>Correction:</strong> {editing.review.correction_raw}</p>:null}</div>:null}{preview?<div className={`rounded-xl border p-4 ${preview.valid?"border-blue-200 bg-blue-50":"border-red-200 bg-red-50"}`}><strong className="text-sm">Impact preview</strong><p className="mt-2 text-sm text-slate-700">Detected HARD violations after this rule change: {preview.hardViolations}. Coverage: {preview.coverage.implementedHardRules}/{preview.coverage.applicableHardRules} applicable HARD rules.</p><p className="mt-1 text-xs text-slate-500">Saving creates a new Rulebook version. The current schedule then becomes stale until explicitly revalidated.</p></div>:null}<label className="text-xs font-semibold text-slate-600">Reason for change<input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Why is studio policy changing?" className="mt-1 min-h-11 w-full rounded-xl border border-slate-300 px-3 text-sm font-normal"/></label>{notice?<p className="text-sm text-red-700">{notice}</p>:null}<div className="flex gap-2"><button onClick={()=>setEditing(null)} className="min-h-11 flex-1 rounded-xl border border-slate-300 font-semibold">Cancel</button><button disabled={saving} onClick={()=>void save()} className="min-h-11 flex-1 rounded-xl bg-slate-950 font-semibold text-white disabled:opacity-40">{saving?"Saving…":original?`Save as Rulebook v${currentRulebookVersion+1}`:"Create rule"}</button></div></div></div></div>:null}

    {historyId&&historyRule?<div className="fixed inset-0 z-[70] flex items-end justify-center bg-slate-950/40 sm:items-center sm:p-6"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-t-[28px] bg-white p-5 shadow-2xl sm:rounded-[28px] sm:p-6"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wider text-slate-500">Rule history</p><h2 className="mt-1 text-xl font-semibold">{historyRule.id} · {historyRule.title}</h2></div><button onClick={()=>setHistoryId(null)} className="rounded-xl p-2"><X className="size-5"/></button></div>{historyRule.review?.original_text?<div className="mt-5 rounded-2xl border border-violet-200 bg-violet-50 p-4"><p className="text-xs font-semibold uppercase tracking-wider text-violet-700">Original human review history</p><p className="mt-3 text-sm"><strong>Draft wording:</strong> {historyRule.review.original_text}</p>{historyRule.review.correction_raw?<p className="mt-2 text-sm"><strong>Reviewer correction:</strong> {historyRule.review.correction_raw}</p>:null}<p className="mt-2 text-sm"><strong>Reviewed V2:</strong> {historyRule.description}</p></div>:null}<div className="mt-5 space-y-3">{history.map(entry=><div key={entry.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-center justify-between"><strong className="text-sm">Rulebook v{entry.rulebookVersion}</strong><span className="text-xs text-slate-400">{new Date(entry.changedAt).toLocaleString()}</span></div><p className="mt-2 text-xs text-slate-600">{entry.reason}</p>{entry.before?.description&&entry.before.description!==entry.after?.description?<div className="mt-3 grid gap-2 text-xs"><p className="rounded-lg bg-red-50 p-2"><strong>Before:</strong> {entry.before.description}</p><p className="rounded-lg bg-emerald-50 p-2"><strong>After:</strong> {entry.after?.description}</p></div>:null}<p className="mt-2 text-[11px] text-slate-400">{entry.actor}{entry.aiProposed?" · AI proposal approved":""}</p></div>)}</div></div></div>:null}
  </div>;
}