# Material execution risks

| Risk | Evidence and impact | Mitigation / milestone |
|---|---|---|
| Deployed recovery and configuration unverified | Prior `OPS-01` ledger and release checklist have a disposable rehearsal, but no authorized staging/deployed migration, solver health, token, restore or write-isolation observations. A local green build cannot close operational readiness. | Obtain owner-authorized environment and record exact deployment/version/restore evidence at M02; never use production for tests. |
| Private manager and independent studio evidence absent | Old `ACC-01`/`GEN-05` and S201–S218 require real consenting managers, private dataset/use evidence and a frozen SHA. Without this, A/B exits and later pilot claims remain unproven. | Owner supplies participants and private artifact references; run M02 acceptance without public identifiable data or a bespoke source patch. |
| Owner-authorized staging environment and observations are unavailable | M01 is frozen at `cb6520d985d3febd2297e01311b298bb65e3c392`, but the local config check found all seven deployment variables absent and OPS-01 staging observations require an authorized environment and owner permission. | Keep M02-T01 blocked until the owner makes approved staging configuration available and authorizes the checklist observations. Do not treat M01 tests as external acceptance. |
| Local Docker needs elevated sandbox access | Default sandbox access to `npipe:////./pipe/docker_engine` is denied; the required M01 DB, browser and restore gates passed when invoked with explicit local Docker access. | Use the disposable harness with approved local access; never fall back to production. M01 passed; M02 still needs its owner-authorized environment. |
| Operator/legal terms and support limits undefined | Prior TASKS records `EXT-PILOT`; account deletion/retention and incident response terms are not established in repository. Unsupported promises could make a pilot unsafe. | M01 documents implementable mechanisms/limits; operator confirms terms and support owner before M03. |
| Historical plan and README claims can be mistaken for current status | Archive contains obsolete task order and old root README describes earlier coverage. Future work could mark product ready from task-level implementation. | Use this directory for status; cite source and gate artifacts. Root planning pointers redirect here. All milestones. |
